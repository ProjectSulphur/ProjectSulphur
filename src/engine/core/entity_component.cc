#include "entity_component.h"

namespace sulphur
{
  namespace engine
  {
	//-------------------------------------------------------------------------
    EntityComponentData::EntityComponentData(bool with_editor) :
      with_editor(with_editor)
    {
    }
  }
}